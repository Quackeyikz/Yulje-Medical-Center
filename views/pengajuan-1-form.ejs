<!DOCTYPE html>
<html lang="id">

<head>
	<meta charset="UTF-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<title>Pendaftaran Jadwal Konsultasi</title>
	
	<%- include("modules/head.ejs") %>
</head>

<body>
	<%- include("modules/navbar.ejs") %>
	
	<main class="max-w-3xl mx-auto mt-10 mb-10 bg-white shadow-lg rounded-2xl overflow-hidden">
		<div class="bg-cyan-400 text-white text-center py-5 text-xl font-bold">Pendaftaran Jadwal Konsultasi</div>
		<div class="p-6">
			<form method="post" action="/proses" class="space-y-6">
				<% if(error_msg === 1) { %>
					<div class="w-full p-4 bg-red-300 text-red-800 rounded-lg font-semibold border border-1 border-red-800">
						Maaf, tidak ada dokter yang tersedia untuk jadwal dan poli yang dipilih.
					</div>
				<% } %>
				<div>
					<h2 class="text-lg font-semibold mb-4">Data Pasien</h2>
					
					<div class="mb-4">
						<label for="nama" class="block mb-1">Nama Lengkap</label>
						<input type="text" id="nama" name="nama" class="w-full border rounded-md px-3 py-2"
						placeholder="Masukkan nama lengkap" <% if(error_msg === 1) { %>value="<%= data.nama %>" <% } %> required>
					</div>
					
					<div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-4">
						<div>
							<label for="tanggal_lahir" class="block mb-1">Tanggal Lahir</label>
							<input type="date" id="tanggal_lahir" name="tanggal_lahir" class="w-full border rounded-md px-3 py-2"
							<% if(error_msg === 1) { %>value="<%= data.tanggal_lahir %>" <% } %> required>
						</div>
						<div>
							<label for="jk" class="block mb-1">Jenis Kelamin</label>
							<select id="jk" name="jk" class="w-full border rounded-md px-3 py-2" required>
								<option value="" disabled selected>Pilih jenis kelamin</option>
								<option value="Pria" <% if(error_msg === 1) { %>selected<% } %>>Pria</option>
								<option value="Wanita">Wanita</option>
							</select>
						</div>
					</div>
					
					<div class="mb-4">
						<label for="kontak" class="block mb-1">Kontak (Nomor Telepon Aktif)</label>
						<input type="tel" id="kontak" <% if(error_msg === 1) { %>value="<%= data.kontak %>" <% } %> name="kontak" class="w-full border rounded-md px-3 py-2" placeholder="+62xxxxxxxxxxx" required>
					</div>
					
					<div class="mb-4">
						<label for="alamat" class="block mb-1">Alamat</label>
						<textarea id="alamat" name="alamat" rows="3" class="w-full border rounded-md px-3 py-2" placeholder="Masukkan alamat lengkap" required><% if(error_msg === 1) { %><%= data.alamat %><% } %></textarea>
					</div>
				</div>
				
				<!-- Jadwal Konsultasi -->
				<div>
					<h2 class="text-lg font-semibold mb-4">Jadwal Konsultasi</h2>
					
					<div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-4">
						<div>
							<label for="tgl_konsultasi" class="block mb-1">Tanggal Konsultasi</label>
							<input type="date" id="tgl_konsultasi" name="tgl_konsultasi" <% if(error_msg === 1) { %>value="<%= data.tgl_konsultasi %>" <% } %> class="w-full border rounded-md px-3 py-2" required>
						</div>
						<div>
							<label for="jam_konsultasi" class="block mb-1">Jam Konsultasi</label>
							<input type="time" id="jam_konsultasi" name="jam_konsultasi" <% if(error_msg === 1) { %>value="<%= data.jam_konsultasi %>" <% } %> class="w-full border rounded-md px-3 py-2" required>
						</div>
					</div>
					
					<div class="mb-4">
						<label for="poli" class="block mb-1">Poli</label>
						<select id="poli" name="poli" class="w-full border rounded-md px-3 py-2" required>
							<option value="" disabled selected>Pilih Poli</option>
							<option value="Umum">Umum</option>
							<option value="Gigi">Gigi</option>
							<option value="Anak">Anak</option>
							<option value="Kulit">Kulit</option>
							<option value="THT">THT</option>
							<option value="Gizi">Gizi</option>
							<option value="Saraf">Saraf</option>
							<option value="Psikiater">Psikiater</option>
							<option value="Kandungan">Kandungan</option>
							<option value="Mata">Mata</option>
							<option value="Jantung">Jantung</option>
							<option value="Bedah">Bedah</option>
							<option value="Paru">Paru</option>
						</select>
					</div>
					
					<div class="mb-4">
						<label for="keluhan" class="block mb-1">Keluhan</label>
						<textarea id="keluhan" name="keluhan" rows="3" class="w-full border rounded-md px-3 py-2" placeholder="Tulis keluhan Anda" required><% if(error_msg === 1) { %><%= data.keluhan %><% } %></textarea>
					</div>
				</div>
				
				<!-- Buttons -->
				<div class="flex justify-end gap-3">
					<button type="reset" class="px-5 py-2 border rounded-full text-gray-700 bg-gray-100 hover:bg-gray-200">Reset</button>
					<button type="submit" class="px-5 py-2 bg-cyan-400 text-white rounded-full hover:bg-cyan-500">Lanjut</button>
				</div>
			</form>
		</div>
	</main>
	
	<%- include("modules/footer.ejs") %>
</body>
</html>